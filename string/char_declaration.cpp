#include <stdio.h>

int main()
{
    char c; // single char. stored as a number in memory ASCII code or UNICODE.
    c = 'a'; // single quotes for single char.

    printf("%c\n", c); // %c for printing char.
    return 0;
}